//! No-Boltha lazy dataframe option enums.
//!
//! These mirror the Boltha-backed lazy enum names so metadata-only code can
//! compile when Arrow/Parquet support is disabled.

pub const DeviceLazyGroupByAggregation = enum {
    sum,
    prod,
    min,
    max,
    mean,
    first,
    last,
    first_row,
    last_row,
    nth,
    nth_row,
    nth_index,
    nth_row_index,
    n_unique,
    mode,
    mode_count,
    mode_ratio,
    mode_margin,
    mode_margin_ratio,
    entropy,
    gini_impurity,
    perplexity,
    inverse_simpson,
    simpson_concentration,
    evenness,
    gini_mean_diff,
    gini_coefficient,
    mean_abs_dev,
    mean_abs_dev_ratio,
    median,
    quantile,
    iqr,
    mad,
    trimmed_mean,
    winsorized_mean,
    interdecile_range,
    midhinge,
    trimean,
    bowley_skewness,
    quartile_coeff_dispersion,
    kelley_skewness,
    variance,
    stddev,
    sem,
    cv,
    fano,
    skewness,
    kurtosis,
    magnitude_variance,
    magnitude_stddev,
    magnitude_sem,
    magnitude_cv,
    magnitude_fano,
    magnitude_skewness,
    magnitude_kurtosis,
    mean_abs,
    mean_square,
    rms,
    l1_norm,
    l2_norm,
    max_abs,
    min_abs,
    hhi,
    magnitude_normalized_hhi,
    magnitude_sparsity,
    magnitude_inverse_simpson,
    magnitude_simpson_evenness,
    magnitude_dominance,
    magnitude_dominance_margin,
    magnitude_entropy,
    magnitude_perplexity,
    magnitude_evenness,
    geometric_mean,
    harmonic_mean,
    logsumexp,
    logmeanexp,
    ptp,
    midrange,
    range_coeff,
    any,
    all,
    true_count,
    false_count,
    true_ratio,
    false_ratio,
    first_true_index,
    last_true_index,
    first_false_index,
    last_false_index,
    any_valid,
    all_valid,
    any_null,
    all_null,
    valid_count,
    null_count,
    valid_ratio,
    null_ratio,
    first_valid_index,
    last_valid_index,
    first_null_index,
    last_null_index,
    nan_count,
    nan_ratio,
    inf_count,
    inf_ratio,
    positive_inf_count,
    positive_inf_ratio,
    negative_inf_count,
    negative_inf_ratio,
    first_nan_index,
    last_nan_index,
    first_inf_index,
    last_inf_index,
    first_positive_inf_index,
    last_positive_inf_index,
    first_negative_inf_index,
    last_negative_inf_index,
    finite_count,
    finite_ratio,
    first_finite_index,
    last_finite_index,
    normal_count,
    normal_ratio,
    first_normal_index,
    last_normal_index,
    subnormal_count,
    subnormal_ratio,
    first_subnormal_index,
    last_subnormal_index,
    non_finite_count,
    non_finite_ratio,
    first_non_finite_index,
    last_non_finite_index,
    zero_count,
    zero_ratio,
    first_zero_index,
    last_zero_index,
    positive_zero_count,
    positive_zero_ratio,
    negative_zero_count,
    negative_zero_ratio,
    first_positive_zero_index,
    last_positive_zero_index,
    first_negative_zero_index,
    last_negative_zero_index,
    non_zero_count,
    non_zero_ratio,
    first_non_zero_index,
    last_non_zero_index,
    positive_count,
    positive_ratio,
    first_positive_index,
    last_positive_index,
    signbit_count,
    signbit_ratio,
    first_signbit_index,
    last_signbit_index,
    negative_count,
    negative_ratio,
    first_negative_index,
    last_negative_index,
    argmin,
    argmax,
};

pub const DeviceLazyWeightedGroupByAggregation = enum {
    weighted_sum,
    weighted_product,
    weighted_weight_sum,
    weighted_positive_count,
    weighted_effective_n,
    weighted_mean,
    weighted_mean_square,
    weighted_rms,
    weighted_min,
    weighted_max,
    weighted_mean_abs,
    weighted_l1_norm,
    weighted_l2_norm,
    weighted_max_abs,
    weighted_min_abs,
    weighted_geometric_mean,
    weighted_harmonic_mean,
    weighted_logsumexp,
    weighted_logmeanexp,
    weighted_range,
    weighted_midrange,
    weighted_range_coeff,
    weighted_variance,
    weighted_stddev,
    weighted_sem,
    weighted_cv,
    weighted_fano,
    weighted_skewness,
    weighted_kurtosis,
    weighted_quantile,
    weighted_median,
    weighted_iqr,
    weighted_mad,
    weighted_trimmed_mean,
    weighted_winsorized_mean,
    weighted_mode,
    weighted_mode_weight,
    weighted_mode_ratio,
    weighted_mode_margin,
    weighted_mode_margin_ratio,
    weighted_entropy,
    weighted_gini_impurity,
    weighted_perplexity,
    weighted_inverse_simpson,
    weighted_simpson_concentration,
    weighted_evenness,
};

pub const DeviceLazyPairGroupByAggregation = enum {
    dot,
    cosine_similarity,
    squared_euclidean_distance,
    euclidean_distance,
    manhattan_distance,
    chebyshev_distance,
    canberra_distance,
    bray_curtis_distance,
    mean_error,
    mae,
    mse,
    rmse,
    mape,
    smape,
    pair_count,
    covariance,
    correlation,
    beta,
};

pub const DeviceLazyWeightedPairGroupByAggregation = enum {
    weighted_dot,
    weighted_cosine_similarity,
    weighted_squared_euclidean_distance,
    weighted_euclidean_distance,
    weighted_manhattan_distance,
    weighted_chebyshev_distance,
    weighted_canberra_distance,
    weighted_bray_curtis_distance,
    weighted_mean_error,
    weighted_mae,
    weighted_mse,
    weighted_rmse,
    weighted_mape,
    weighted_smape,
    weighted_covariance,
    weighted_correlation,
    weighted_beta,
};

pub const DeviceLazyJoinKind = enum {
    inner,
    left,
    full,
    semi,
    anti,
};

pub const DeviceLazyOp = union(enum) {
    unsupported: void,
};

pub const DeviceLazySource = union(enum) {
    unsupported: void,
};
